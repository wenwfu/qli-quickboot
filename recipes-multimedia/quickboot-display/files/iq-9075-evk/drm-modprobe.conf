# Configure the DRM module to skip GPU initialization dependency.
# This allows the display controller to initialize and show early splash/UI
# before the full GPU stack is completely ready.

options drm skip_gpu=1
